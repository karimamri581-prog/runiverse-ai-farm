name: DOM Playground Econ Agent
on:
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch:

jobs:
  farm:
    runs-on: ubuntu-latest
    timeout-minutes: 350
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install Dependencies
        run: |
          pip install playwright
          python -m playwright install --with-deps chromium
      - name: Run Playground Econ Agent
        run: |
          python dom_playground_econ.py "https://runiverseidle.com/forge" \
            --goal "log in; enter game" \
            --field email=karimamri581@gmail.com \
            --field password=Taetae1997*
      - name: Save Agent Memory
        if: always()
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add playground_memory.json || echo "No memory file yet"
          git commit -m "Save Playground Econ Memory" || echo "No changes to commit"
          git push
